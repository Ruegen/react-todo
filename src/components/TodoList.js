import React, { hasState, hasEffect } from 'react'

const TodoList = () => {
    return <>
    <form className="todo-form"></form>
    <ul className="todo-list"></ul>
    </>
}

export default TodoList